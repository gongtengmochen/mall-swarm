spring:
  profiles:
    active: dev
  application:
    name: mall-ai
  cloud:
    nacos:
      discovery:
        server-addr: http://localhost:8848
      config:
        server-addr: http://localhost:8848
        file-extension: yaml
        group: DEFAULT_GROUP